{
  "name": "minimal validator test",
  "description": "Minimal test to verify the test system can load JSON test vectors.",
  "testCases": [
    {
      "input": "true",
      "expected": { "isValid": true },
      "because": "Input is true, so it should be valid."
    }
  ]
}